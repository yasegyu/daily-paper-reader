# arXiv 专题回溯

尚无已完成的回溯结果。请在订阅面板选择“回溯九十天 arXiv”或“回溯一年 arXiv”，并选择需要检索的词条。

该模式只读取 arXiv 论文库，按关键词及语义候选调用 DeepSeek 评审，不保证找全。费用随候选量增长，不下载全量 PDF。

完成后这里会列出结果入口，支持按专题、核心/补充/待复核分类分页阅读。未完成的任务可以重跑；在评审缓存仍可用且论文、专题和模型未改变时，已完成评分会复用。

工作流同时提供 `arxiv-long-range-results` 结果附件。主仓库按约定不提交个性化站点产物时，请从该附件下载；Fork 的站点结果仍按原发布流程更新。下载包如需使用交互分页，请解压后用本地 HTTP 服务打开，而不是直接以 `file://` 打开 HTML。

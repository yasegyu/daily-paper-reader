---
title: "Semantic-TVM: Structure-Preserving Trustworthy Virtual Memory for Memory-Augmented and Tool-Using Agents"
title_zh: Semantic-TVM：面向记忆增强和工具使用智能体的结构保持可信虚拟内存
authors: "Yu Li, Qikun Cai, Tao Huang, Chen Hou"
date: 2026-09-14
pdf: "https://arxiv.org/pdf/2609.15011v1"
tags: ["query:agent-memory"]
score: 7.0
evidence: 面向记忆增强与工具使用智能体的可信虚拟内存
tldr: "记忆增强与工具型智能体在远程 LLM 处理检索记忆、工具动作和中间观察时易暴露精确隐私值，单向掩码又破坏可信执行所需值并可能经后续观察泄露。为此提出可信虚拟内存 TVM，闭环运行时将精确值状态保留本地，仅向远程模型呈现受保护视图；Semantic-TVM 用可信本地模型预测敏感片段并只替换这些片段，保留任务相关上下文。在 Memory-EHR 与 Memory-RAP 上，片段级投影恢复大部分因整字段替换损失的 EHR 效用，DeepSeek 任务成功率 84.17% 对 52.33%，暴露低且工作流可执行。该工作为记忆增强和工具型智能体提供结构保留、低暴露且可执行的可信内存方案。"
source: arxiv
selection_source: fresh_fetch
motivation: 记忆增强与工具型智能体需远程 LLM 处理检索记忆、工具动作和中间观察，精确隐私值因此暴露；单向掩码又牺牲可信执行所需值并可能经后续观察泄露。
method: 提出可信虚拟内存 TVM：闭环运行时将精确值状态留本地，向远程模型呈现受保护视图；Semantic-TVM 用可信本地模型预测敏感片段，只替换敏感 span，保留任务上下文。
result: "在 Memory-EHR 和 Memory-RAP 两数据集、两供应商上，片段级投影恢复大部分整字段替换损失的 EHR 效用，DeepSeek 任务成功率 84.17% 对 52.33%，暴露低且工作流可执行。"
conclusion: 为记忆增强和工具型智能体提供结构保留、低暴露且可执行的可信内存运行时，平衡隐私与效用。
---

## 摘要
当远程LLM处理检索到的记忆、工具动作和中间观察结果时，记忆增强和工具使用智能体会暴露精确的私有值。单向掩蔽限制了直接暴露，但会移除可信执行所需的值，并可能通过后续观察泄漏它们。我们提出可信虚拟内存（TVM），一种闭环运行时，将精确值状态保留在本地，同时向远程模型呈现受保护的视图。在这一单一运行时内，Rule-TVM用本地可恢复的句柄替换整个受保护字段，而Semantic-TVM仅替换由可信本地模型预测的敏感片段，保留周围与任务相关的上下文。在两个提供商的Memory-EHR和Memory-RAP上，片段级投影恢复了在整字段替换下损失的大部分EHR效用（在DeepSeek上任务成功率为84.17% vs. 52.33%），同时测得暴露保持较低，且工作流仍可执行。

## Abstract
Memory-augmented and tool-using agents expose exact private values when remote LLMs process retrieved memory, tool actions, and intermediate observations. One-way masking limits direct exposure but removes values needed for trusted execution and can leak them through later observations. We propose Trustworthy Virtual Memory (TVM), a closed-loop runtime that keeps exact-value state local while presenting a protected view to the remote model. Within this single runtime, Rule-TVM replaces whole protected fields with locally recoverable handles, and Semantic-TVM instead replaces only sensitive spans predicted by a trusted local model, preserving surrounding task-relevant context. On Memory-EHR and Memory-RAP across two providers, span-level projection recovers most of the EHR utility lost under whole-field replacement (Task Success 84.17% vs. 52.33% on DeepSeek) while measured exposure stays low and workflows remain executable.
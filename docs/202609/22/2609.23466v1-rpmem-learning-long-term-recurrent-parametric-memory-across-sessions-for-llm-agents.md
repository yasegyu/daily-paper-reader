---
title: "RPMem: Learning Long-Term Recurrent Parametric Memory Across Sessions for LLM Agents"
title_zh: RPMem：面向LLM代理的跨会话长时循环参数记忆学习
authors: "Fanyu Zhao, Ruike Cao, Liang Dong, Fugen Yao, Jian Xu, Guanjun Jiang, Han Zhang, Yifei Zhao, Yinsheng Li"
date: 2026-09-20
pdf: "https://arxiv.org/pdf/2609.23466v1"
tags: ["query:agent-memory"]
score: 9.0
evidence: 跨会话参数化记忆存储与检索经验
tldr: "长期运行的LLM代理需要跨会话持久进化的记忆，但文本记忆随历史增长依赖检索质量，参数记忆受限于骨干模型且难以跨会话演化。RPMem采用两阶段架构，先将会话编译为模型无关的潜在记忆，再通过任务训练的门控选择性整合至保留记忆，最终映射为骨干特定的低秩适配参数以支持模型替换。在三个长期记忆基准和五个骨干上验证，Qwen3-8B在PERMA上达85.52%，优于最强基线的参数和文本方法，且更新成本与内存占用近恒定。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.23466v1/fig-001.webp\", \"caption\": \"\", \"page\": 1, \"index\": 1, \"width\": 2750, \"height\": 183}, {\"url\": \"assets/figures/arxiv/2609.23466v1/fig-002.webp\", \"caption\": \"\", \"page\": 2, \"index\": 2, \"width\": 512, \"height\": 512}, {\"url\": \"assets/figures/arxiv/2609.23466v1/fig-003.webp\", \"caption\": \"\", \"page\": 2, \"index\": 3, \"width\": 512, \"height\": 512}, {\"url\": \"assets/figures/arxiv/2609.23466v1/fig-004.webp\", \"caption\": \"\", \"page\": 2, \"index\": 4, \"width\": 512, \"height\": 512}, {\"url\": \"assets/figures/arxiv/2609.23466v1/fig-005.webp\", \"caption\": \"\", \"page\": 2, \"index\": 5, \"width\": 512, \"height\": 512}, {\"url\": \"assets/figures/arxiv/2609.23466v1/fig-006.webp\", \"caption\": \"\", \"page\": 2, \"index\": 6, \"width\": 512, \"height\": 512}, {\"url\": \"assets/figures/arxiv/2609.23466v1/fig-007.webp\", \"caption\": \"\", \"page\": 2, \"index\": 7, \"width\": 512, \"height\": 512}, {\"url\": \"assets/figures/arxiv/2609.23466v1/fig-008.webp\", \"caption\": \"\", \"page\": 2, \"index\": 8, \"width\": 512, \"height\": 512}, {\"url\": \"assets/figures/arxiv/2609.23466v1/fig-009.webp\", \"caption\": \"\", \"page\": 2, \"index\": 9, \"width\": 512, \"height\": 512}, {\"url\": \"assets/figures/arxiv/2609.23466v1/fig-010.webp\", \"caption\": \"\", \"page\": 2, \"index\": 10, \"width\": 512, \"height\": 512}, {\"url\": \"assets/figures/arxiv/2609.23466v1/fig-011.webp\", \"caption\": \"\", \"page\": 2, \"index\": 11, \"width\": 512, \"height\": 512}]"
motivation: 现有LLM代理记忆方法难以同时支持跨会话演化与骨干模型替换，需要一种生命周期独立且可复用的参数记忆机制。
method: RPMem通过前向计算将每会话编译为模型无关的潜在记忆，用任务训练的循环门控选择性整合保留记忆，再映射为骨干特定的LoRA参数。
result: "在PERMA等三个长期记忆基准和五种骨干上，RPMem以近恒定成本达到85.52%的准确率，超越最强基线5.32和12.98个百分点。"
conclusion: RPMem实现跨会话演化的参数记忆框架，记忆可跨骨干复用，门控习得任务相关的整合策略，验证了会话编译与跨会话巩固的互补作用。
---

## 摘要
长期运行的LLM代理需要跨会话持久存在并不断演进的记忆。基于文本的记忆在每次查询时检索并重建过去的交互，随着历史增长，长时程性能越来越依赖于检索质量和上下文推理。参数化记忆直接将经验编码到模型计算中，但现有方法对跨会话记忆演进的支持有限。它们与特定骨干网络的耦合进一步限制了模型替换后记忆的复用。我们引入RPMem，一种两阶段架构，通过前向计算将会话编译为与模型无关的潜在记忆，并通过任务训练的循环门控将其与保留记忆选择性整合。整合后的记忆随后映射到骨干网络特定的低秩适应（LoRA）参数，使得编码能力在骨干网络替换时能够迁移。在三个长时记忆基准和五个不同骨干网络上的评估展示了广泛的泛化能力，且更新成本和记忆占用几乎恒定。在PERMA上使用Qwen3-8B时，RPMem达到85.52%，分别超越最强的参数化和基于文本的基线5.32和12.98个百分点。消融研究验证了会话编译和跨会话整合的互补作用，而动态分析揭示门控习得了任务特定的记忆整合策略。这些结果确立了RPMem作为一个生命周期无关的参数化记忆框架，能够维护跨会话演进的记忆，并在骨干网络替换后保持可复用。我们的实现可在https://github.com/Quark-Medical/rpmem/tree/main获取。

## Abstract
Long-running LLM agents require memory that persists and evolves across sessions. Text-based memory retrieves and reconstructs past interactions at every query, making long-horizon performance increasingly dependent on retrieval quality and contextual reasoning as histories grow. Parametric memory encodes experience directly into model computation, but existing approaches provide limited support for cross-session memory evolution. Their coupling to a specific backbone further restricts memory reuse after model replacement. We introduce RPMem, a two-stage architecture that compiles each session into a model-independent latent memory through forward computation and selectively integrates it with retained memory via a task-trained recurrent gate. The consolidated memory is then mapped to backbone-specific low-rank adaptation (LoRA) parameters, allowing the encoding capability to transfer when the backbone is replaced. Evaluation across three long-term memory benchmarks and five diverse backbones demonstrates broad generalization with near-constant update cost and memory footprint. With Qwen3-8B on PERMA, RPMem reaches 85.52%, outperforming the strongest parametric and text-based baselines by 5.32 and 12.98 percentage points, respectively. Ablations validate the complementary roles of session compilation and cross-session consolidation, while dynamics analyses reveal that the gate acquires task-specific memory integration strategies. These results establish RPMem as a lifecycle-independent parametric memory framework that maintains evolving cross-session memory that remains reusable across backbone replacements. Our implementation is available at https://github.com/Quark-Medical/rpmem/tree/main.

---

## 论文详细总结（自动生成）

## 论文总结

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：长期运行的LLM代理需要在跨会话场景下保持连续性，这种连续性依赖于记忆系统在两个方面的表现：一是在持续交互中保留历史情境，二是在任务执行中保留先前的决策、约束和结果。因此，长期记忆成为代理架构的关键能力。
- **现有方法的两大局限**：
  - **文本记忆方法**（如Mem0、LightMem等）：将交互存储为外部文本，每次查询都需检索并重建过去交互。这种方法的缺陷是长时程性能依赖于检索质量和上下文推理，且随着历史增长，不完整的检索可能遗漏必要信息，导致推理不完整。
  - **参数化记忆方法**（如MEMORYLLM、M+、Metis等）：直接将经验编码到模型计算中，但存在两个问题：一是对跨会话记忆演进的支持有限，二是记忆被耦合到特定的骨干网络架构上，当服务模型被替换时，积累的记忆无法复用。
- **核心问题**：论文定义了"生命周期无关性"（lifecycle independence）这一概念，即保持和继续使用积累的记忆跨越多代服务骨干的替换。这被认为是持久参数记忆的关键未解决障碍。
- **重要性**：在实际部署系统中，服务模型被常规替换，如果记忆与特定骨干耦合，每次模型升级都会导致记忆积累失败，无法支撑真正的长期代理应用。

### 2. 论文提出的方法论：核心思想、关键技术细节、算法流程

**RPMem（Recurrent Parametric Memory，循环参数记忆）**是一个两阶段循环架构，核心思想是将记忆以模型无关的潜在空间存储，并通过模型特定的解码器映射到骨干网络。

- **单会话记忆编译**（Single-Session Memory Compilation）：
  - 使用**冻结的上下文编码器**（如ModernBERT）提取多深度特征
  - 通过**Perceiver-based重采样器**将变长上下文压缩为固定形状的会话记忆 q_t（形状为 L×M×r×d，与会话长度无关）
  - 使用**LoRA解码器**将会话记忆映射为骨干网络特定的LoRA参数（A、B因子对）
  - 通过将记忆增强的骨干网络的下一个token分布与同一骨干网络读取文本上下文时的分布进行对齐来训练，使用**前向KL散度**（式9）作为对齐损失

- **跨会话记忆巩固**（Cross-Session Memory Consolidation）：
  - 使用一个轻量级**循环巩固门控**，参数只有2d²+d个，与模型层数、模块类型和秩无关
  - 门控计算保留向量 z_t = σ([h_t−1; q_t]W_g + b_g)，通过 h_t = z_t ⊙ h_t−1 + (1−z_t) ⊙ q_t 更新记忆
  - 通过下游任务的监督信号（如问答交叉熵损失）训练门控，学习如何选择性整合新会话信息与已有记忆

- **推理阶段**：所有模块冻结，新会话通过前向计算完成记忆更新，查询时通过解码器将累计记忆映射为LoRA参数供骨干使用

- **模型替换**：保留记忆、会话编码器和已学习的门控，仅替换为新骨干对应的模型特定解码器，即可复用积累的记忆

### 3. 实验设计：数据集、基准场景和对比方法

**三个长期记忆基准**：
- **PERMA**：评估有序会话间的记忆形成、修订和干预能力，包含单域（SD）和多域（MD）、干净和噪声等变体，共7个评估环境，采用十折留一用户评估协议
- **PersonaMem-v2**：从长用户历史中派生独立查询，报告Overall、Self、Current准确率
- **Pref Eval**：测试经过10、70、300轮干预对话后的记忆保持能力

**对比方法**：
- **文本记忆基线**：Full Context、RAG（BGE-M3）、Rolling Summary、Mem0、LightMem
- **参数化基线**：SFT(领域历史微调)、Metis-9B(发布的最强循环记忆模型)
- **参考条件**：No Context（仅查询）、Gold State（访问标注记忆）

**骨干网络**：Qwen3-8B（主）、Qwen3-4B、Ministral-3-8B、Qwen3.5-9B、Qwen3.5-35B-A3B（跨模型泛化）

### 4. 资源与算力

论文明确报告了以下算力成本：

- **编译阶段**：
  - 共享编译器（Qwen3-8B，Fixed FKL目标）需要**338 GPU小时**，使用8个GPU，51,885次更新
  - 其他训练目标成本更高：Sampled FKL为5,284 GPU小时，Fixed RKL为2,939 GPU小时

- **巩固阶段**：
  - 典型PERMA折叠只需要**13分钟**，使用单个A800-80GB GPU训练
  - 可训练参数仅524,800个，3,150次更新（5轮）

- **跨骨干转移**：Qwen3-4B和Ministral-3-8B各需约7-8 GPU小时（8 GPU），Qwen3.5-9B约32小时，Qwen3.5-35B-A3B约42小时（32 GPU）

- **部署效率**：64个会话语义后，每次写入0.043秒，增长1.03×；持久记忆36.56 MiB，增长1.00×；无历史查询token

### 5. 实验数量与充分性

论文实验非常充分，包含：

- **三大基准测试**：PERMA（7个变体×10折）、PersonaMem-v2、Pref Eval（3种证据形式×3种历史长度）
- **五个骨干网络的性能对比**（PERMA整体平均） 
- **编译目标消融**：6种目标（Compiler SFT、Sampled RKL(top-K)、Sampled RKL(K3)、Top-K CE、Sampled FKL、Fixed FKL）
- **巩固规则消融**：5种规则（Rank concatenation、Latent averaging、Latest Session、Factor averaging、Learned gate）
- **跨骨干转移实验**：4个目标骨干×7个设置×2种初始方式（From scratch vs Head transfer）
- **消融相关动态分析**：写入模式、域结构、长期存活率
- **一般能力保持测试**：MMLU、GSM8K、IFEval
- **训练数据效率研究**：1-9个训练用户的适应曲线
- **与Metis的同骨干对比**（Qwen3.5-9B）
- **与Metis的规模对比**（4B/9B/27B）

**公平性评估**：论文检查了No Context条件下API生成与本地logits的一致性、官方手动prompt与自定义prompt的结果差异，并区分了论文复现结果和基准原文报告的结果。各方法共享相同历史边界、查询和候选答案接口。

### 6. 论文的主要结论与发现

- **性能优势**：在PERMA上使用Qwen3-8B达到85.52%的准确率，超越Metis-9B 5.32 pp、Full Context 12.98 pp、SFT 25.17 pp
- **跨骨干泛化**：五个骨干上整体改进5.95-20.41 pp，验证了机制可在异构LM骨干上泛化
- **生命周期无关性**：冻结的Qwen3-8B内存编译器可复用于其他4个骨干（仅拟合特定解码器），转移在28种骨干-设置组合上全部优于从零训练，平均从75.97%提升至89.64%
- **两种训练阶段的互补性**：编译目标中Fixed FKL最优（85.52%），巩固门控优于所有固定规则（最强固定规则为LoRA因子均值54.21%，门控为85.52%）
- **门控学习到的动态**：门控能够区分域形成事件和补充事件，域形成事件获得更大写入量并存活更久（1步后1.31倍、60步后6.64倍），同域更新会选择性修订相关记忆而跨域信息被保留
- **部署可行性**：更新成本近恒定（约1.03×），记忆大小不变，一般能力保持（MMLU差距1.83pp、GSM8K差距1.43pp、IFEval差距3.81pp）

### 7. 优点

- **创新性问题定义**：明确提出"生命周期无关参数记忆"问题，填补了参数记忆在骨干替换场景下的空白
- **两阶段训练设计**：单一会话编译与跨会话巩固分离，便于针对目标任务调整记忆策略
- **模型不可知记忆表示**：通过独立的编码器和解码器设计，使记忆本质上是骨干不可知的，只需要替换解码器
- **高效部署**：所有阶段通过前向计算完成内存更新，避免重复梯度优化；巩固阶段仅需13分钟训练
- **固定大小记忆**：记忆大小不随会话数增长，后续迭代开销近恒定
- **充分的消融和动态分析**：不仅报告性能，还深入分析了门控学习到的语义结构，提供了内部机制的直觉理解
- **实验设计严谨**：覆盖五种骨干、三个基准、七种PERMA变体、动态分析和部署效率测量

### 8. 不足与局限

- **应用场景限定**：巩固策略是针对特定场景训练的，对其他场景适用性未研究。论文自己承认这一局限性："应用特定的巩固使记忆保留和更新与目标场景需求对齐，但学习到的策略对记忆需求大幅不同的场景的适用性仍需研究"
- **PERMA用户规模有限**：PERMA只有10个模拟用户，虽然使用十折验证，但用户多样性的代表性问题仍存疑
- **实验评估依赖模拟基准**：三个主基准均属于模拟设定，可能未完全反映真实用户交互的复杂性和多样性
- **参数量问题**：跨骨干迁移实验显示From scratch（从头训练）需要580.67M-659.72M参数，Head transfer（头转移）也需要532.40M-611.45M参数，没有说明在资源受限场景中的适用性
- **编译数据的可能偏差**：编译语料使用特定对话来源（UltraChat、Taskmaster等），也许影响会话编码质量的普遍性
- **固定门控策略**：巩固门控在部署后参数固定，不能适应环境或任务需求随着时间的变化
- **跨模型可比性限制**：比较 Release 的 Metis-9B 时，RPMem 使用 Qwen3-8B，虽然附录中提供了同骨干对比（Qwen3.5-9B），但这不是所有基线都同意的主要比较

（完）
